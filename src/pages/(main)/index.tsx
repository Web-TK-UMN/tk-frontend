import { Stack, Text } from "@chakra-ui/react";

const IndexPage = () => {
  return (
    <Stack h={"100vh"} align={"center"} justify={"center"}>
      <Text>Ini frontend yang ditampilkan ke user</Text>
    </Stack>
  );
};

export default IndexPage;
